#!/usr/bin/env bash
# Documented `nexus` wrapper — execs python3 -m nexus_harness.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PYTHONPATH="$ROOT/src${PYTHONPATH:+:${PYTHONPATH}}" exec python3 -m nexus_harness "$@"
